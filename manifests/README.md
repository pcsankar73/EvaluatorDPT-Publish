# Manifests

Pointer files that enumerate where release artifacts live (e.g., storage URLs for checkpoints and tokenized datasets).

These manifests intentionally do not include credentials or time-limited access tokens.

